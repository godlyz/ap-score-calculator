# v3.1 Dynamic Sync Fix Report

## Summary
Fixed the APUSH v3.1 dashboard so the hero result card, score band marker, and Target 3/4/5 cards now all update from the same `calculateScore()` output. The APUSH dashboard state is now synchronized across the main result panel and the visible dashboard preview.

## Changes
- Added stable data hooks to the APUSH dashboard sections in `src/site.js`.
- Updated the browser runtime in `scripts/build.js` so score updates re-render the dashboard preview, score band strip, and target cards together.
- Kept the APUSH result panel and dashboard preview aligned on the same result payload.
- Added/updated tests in `test/site.test.js` to verify dashboard sync markers and runtime linkage.

## Verification
- `npm run check` ✅
- Build output regenerated into `dist/`
- 14/14 tests passing

## Notes
- No production deploy was performed.
- SEO/legal pages and existing homepage hub routing were preserved.
