# Semrush Keyword Gap Summary — AP Score Calculator

> Captured: 2026-05-16 via `sem.3ue.co` Semrush Keyword Gap, US database, desktop, organic keywords, date `20260514`.  
> Target: `apscorecalculator.store`.  
> Required competitor set handled in two Semrush batches because the UI accepts only 3 competitors per comparison:
> Batch 1: `test-ninjas.com`, `knowt.com`, `gradgpt.com`.  
> Batch 2: `num8ers.com`, `fiveable.me`, `albert.io`.  
> Raw capture: `docs/research/semrush-keyword-gap-raw-2026-05-16.json`.

## Caveat

- Semrush UI supports at most 3 competitors in one run, so this summary merges two runs rather than one 6-competitor export.
- Rows below are prioritized by relevance and US search volume; raw file keeps page text and parsed rows for traceability.
- Competitor URL was not exposed in the captured table view; ranking positions by competitor domain were captured. URL-level follow-up should use Semrush SERP/Organic pages for selected keywords.

## Missing keywords — competitors rank, we do not

1. **ap exam schedule** — volume `60.5K`, KD `23`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `44`, knowt.com `53`, gradgpt.com `21`
   - Proposed action: Create/support an AP exam dates or score-release explainer only if aligned with scope; otherwise add FAQ module on hub.
2. **ap test schedule** — volume `27.1K`, KD `25`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `8`, knowt.com `36`, gradgpt.com `21`
   - Proposed action: Create/support an AP exam dates or score-release explainer only if aligned with scope; otherwise add FAQ module on hub.
3. **test grade calculator** — volume `22.2K`, KD `60`, intent `I`
   - Rankings evidence: ours `0`, num8ers.com `32`, fiveable.me `63`, albert.io `19`
   - Proposed action: Watchlist; broad grade-calculator intent may be off-scope unless building a separate utility cluster.
4. **ap score calculator** — volume `14.8K`, KD `31`, intent `C`
   - Rankings evidence: ours `0`, test-ninjas.com `1`, knowt.com `5`, gradgpt.com `8`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
5. **ap score distribution 2025** — volume `14.8K`, KD `43`, intent `N`
   - Rankings evidence: ours `0`, test-ninjas.com `45`, knowt.com `32`, gradgpt.com `41`
   - Proposed action: Create score-distribution section/page with 2025/2026 data and link from hub + subject pages.
6. **apush score calculator** — volume `14.8K`, KD `8`, intent `C/I`
   - Rankings evidence: ours `0`, test-ninjas.com `3`, knowt.com `2`, gradgpt.com `5`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
7. **test calculator** — volume `14.8K`, KD `62`, intent `C`
   - Rankings evidence: ours `0`, test-ninjas.com `15`, knowt.com `17`, gradgpt.com `35`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
8. **ap lang score calculator** — volume `12.1K`, KD `18`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `4`, knowt.com `3`, gradgpt.com `2`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
9. **ap lang calculator** — volume `9.9K`, KD `17`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `4`, knowt.com `3`, gradgpt.com `2`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
10. **sat exam calendar** — volume `9.9K`, KD `44`, intent `I`
   - Rankings evidence: ours `0`, num8ers.com `8`, fiveable.me `57`, albert.io `14`
   - Proposed action: Watchlist or supporting content; validate SERP intent before building.
11. **when do ap scores come out 2025** — volume `9.9K`, KD `35`, intent `I`
   - Rankings evidence: ours `0`, num8ers.com `29`, fiveable.me `56`, albert.io `41`
   - Proposed action: Create/support an AP exam dates or score-release explainer only if aligned with scope; otherwise add FAQ module on hub.
12. **ap calculator** — volume `8.1K`, KD `28`, intent `C`
   - Rankings evidence: ours `0`, test-ninjas.com `1`, knowt.com `7`, gradgpt.com `3`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
13. **ap bio score calculator** — volume `6.6K`, KD `4`, intent `C/I`
   - Rankings evidence: ours `0`, test-ninjas.com `4`, knowt.com `2`, gradgpt.com `5`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
14. **ap chem score calculator** — volume `6.6K`, KD `3`, intent `C/I`
   - Rankings evidence: ours `0`, test-ninjas.com `4`, knowt.com `2`, gradgpt.com `3`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
15. **ap world calculator** — volume `6.6K`, KD `6`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `4`, knowt.com `2`, gradgpt.com `5`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
16. **ap score distribution** — volume `6.6K`, KD `48`, intent `I/N`
   - Rankings evidence: ours `0`, num8ers.com `6`, fiveable.me `20`, albert.io `8`
   - Proposed action: Create score-distribution section/page with 2025/2026 data and link from hub + subject pages.
17. **calculator for test grades** — volume `6.6K`, KD `52`, intent `I`
   - Rankings evidence: ours `0`, num8ers.com `30`, fiveable.me `87`, albert.io `11`
   - Proposed action: Watchlist; broad grade-calculator intent may be off-scope unless building a separate utility cluster.
18. **ap calc ab score calculator** — volume `5.4K`, KD `7`, intent `C/I`
   - Rankings evidence: ours `0`, test-ninjas.com `2`, knowt.com `3`, gradgpt.com `6`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
19. **ap calc bc score calculator** — volume `5.4K`, KD `14`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `2`, knowt.com `3`, gradgpt.com `21`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
20. **ap gov score calculator** — volume `5.4K`, KD `11`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `7`, knowt.com `3`, gradgpt.com `6`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
21. **ap lit score calculator** — volume `5.4K`, KD `14`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `2`, knowt.com `5`, gradgpt.com `7`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
22. **ap world score calculator** — volume `5.4K`, KD `6`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `5`, knowt.com `2`, gradgpt.com `3`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
23. **test score calculator** — volume `5.4K`, KD `59`, intent `I`
   - Rankings evidence: ours `0`, num8ers.com `43`, fiveable.me `91`, albert.io `6`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
24. **ap calc score calculator** — volume `4.4K`, KD `8`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `3`, knowt.com `4`, gradgpt.com `5`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
25. **ap exam calculator** — volume `4.4K`, KD `28`, intent `C`
   - Rankings evidence: ours `0`, test-ninjas.com `1`, knowt.com `6`, gradgpt.com `3`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
26. **ap stats score calculator** — volume `4.4K`, KD `7`, intent `C/I`
   - Rankings evidence: ours `0`, test-ninjas.com `8`, knowt.com `2`, gradgpt.com `4`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
27. **ap united states history practice test** — volume `4.4K`, KD `30`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `10`, knowt.com `18`, gradgpt.com `51`
   - Proposed action: Watchlist or supporting content; validate SERP intent before building.
28. **apes score calculator** — volume `4.4K`, KD `5`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `5`, knowt.com `2`, gradgpt.com `3`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
29. **apush calculator** — volume `4.4K`, KD `10`, intent `C/I`
   - Rankings evidence: ours `0`, test-ninjas.com `3`, knowt.com `2`, gradgpt.com `4`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
30. **chem ap score calculator** — volume `4.4K`, KD `7`, intent `C/I`
   - Rankings evidence: ours `0`, test-ninjas.com `4`, knowt.com `2`, gradgpt.com `3`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
31. **chemistry ap score calculator** — volume `4.4K`, KD `3`, intent `C/I`
   - Rankings evidence: ours `0`, test-ninjas.com `3`, knowt.com `2`, gradgpt.com `4`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
32. **what time do ap scores come out 2025** — volume `4.4K`, KD `30`, intent `I`
   - Rankings evidence: ours `0`, num8ers.com `38`, fiveable.me `76`, albert.io `34`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
33. **ap calculus ab frq** — volume `3.6K`, KD `35`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `17`, knowt.com `39`, gradgpt.com `47`
   - Proposed action: Watchlist or supporting content; validate SERP intent before building.
34. **ap calculus bc score calculator** — volume `3.6K`, KD `5`, intent `C/I`
   - Rankings evidence: ours `0`, test-ninjas.com `2`, knowt.com `3`, gradgpt.com `49`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
35. **ap physics 1 score calculator** — volume `3.6K`, KD `15`, intent `C/I`
   - Rankings evidence: ours `0`, test-ninjas.com `5`, knowt.com `6`, gradgpt.com `3`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.

## Weak keywords — we rank worse than competitors

1. **ap psych calculator** — volume `5.4K`, KD `8`, intent `C/I`
   - Rankings evidence: ours `83`, test-ninjas.com `6`, knowt.com `2`, gradgpt.com `3`
   - Proposed action: Improve existing ranking page: retitle/H1, add target-score guidance, scoring table, FAQ, and stronger internal links.
2. **ap gov calculator** — volume `3.6K`, KD `13`, intent `I`
   - Rankings evidence: ours `93`, test-ninjas.com `8`, knowt.com `2`, gradgpt.com `6`
   - Proposed action: Improve existing ranking page: retitle/H1, add target-score guidance, scoring table, FAQ, and stronger internal links.
3. **ap us gov score calculator** — volume `2.9K`, KD `4`, intent `I`
   - Rankings evidence: ours `53`, test-ninjas.com `8`, knowt.com `2`, gradgpt.com `6`
   - Proposed action: Improve existing ranking page: retitle/H1, add target-score guidance, scoring table, FAQ, and stronger internal links.
4. **ap gov calculator score** — volume `2.4K`, KD `10`, intent `I`
   - Rankings evidence: ours `43`, test-ninjas.com `8`, knowt.com `3`, gradgpt.com `7`
   - Proposed action: Improve existing ranking page: retitle/H1, add target-score guidance, scoring table, FAQ, and stronger internal links.
5. **ap language and composition score calculator** — volume `2.4K`, KD `11`, intent `I`
   - Rankings evidence: ours `76`, test-ninjas.com `4`, knowt.com `2`, gradgpt.com `3`
   - Proposed action: Improve existing ranking page: retitle/H1, add target-score guidance, scoring table, FAQ, and stronger internal links.
6. **ap gov curve** — volume `170`, KD `19`, intent `I`
   - Rankings evidence: ours `53`, test-ninjas.com `3`, knowt.com `10`, gradgpt.com `12`
   - Proposed action: Improve existing ranking page: retitle/H1, add target-score guidance, scoring table, FAQ, and stronger internal links.
7. **apush ap test calculator** — volume `110`, KD `8`, intent `C/I`
   - Rankings evidence: ours `55`, test-ninjas.com `3`, knowt.com `2`, gradgpt.com `7`
   - Proposed action: Improve existing ranking page: retitle/H1, add target-score guidance, scoring table, FAQ, and stronger internal links.
8. **ap gov exam calc** — volume `70`, KD `9`, intent `I`
   - Rankings evidence: ours `75`, test-ninjas.com `5`, knowt.com `2`, gradgpt.com `6`
   - Proposed action: Improve existing ranking page: retitle/H1, add target-score guidance, scoring table, FAQ, and stronger internal links.
9. **ap gov scoring calculator** — volume `50`, KD `8`, intent `I`
   - Rankings evidence: ours `44`, test-ninjas.com `7`, knowt.com `2`, gradgpt.com `8`
   - Proposed action: Improve existing ranking page: retitle/H1, add target-score guidance, scoring table, FAQ, and stronger internal links.

## Untapped keywords — competitors rank, we and at least one batch competitor set are absent/low

1. **ap classroom** — volume `1.5M`, KD `78`, intent `N`
   - Rankings evidence: ours `0`, knowt.com `32`
   - Proposed action: Watchlist or supporting content; validate SERP intent before building.
2. **college board** — volume `1M`, KD `67`, intent `N`
   - Rankings evidence: ours `0`, fiveable.me `43`
   - Proposed action: Watchlist or supporting content; validate SERP intent before building.
3. **grade calculator** — volume `673K`, KD `56`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `72`
   - Proposed action: Watchlist; broad grade-calculator intent may be off-scope unless building a separate utility cluster.
4. **sat test** — volume `368K`, KD `90`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `14`
   - Proposed action: Watchlist or supporting content; validate SERP intent before building.
5. **final grade calculator** — volume `135K`, KD `42`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `82`, knowt.com `40`
   - Proposed action: Watchlist; broad grade-calculator intent may be off-scope unless building a separate utility cluster.
6. **algebra calculator** — volume `110K`, KD `75`, intent `I`
   - Rankings evidence: ours `0`, albert.io `91`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
7. **ap bio** — volume `90.5K`, KD `77`, intent `I`
   - Rankings evidence: ours `0`, knowt.com `42`
   - Proposed action: Watchlist or supporting content; validate SERP intent before building.
8. **grade calc** — volume `74K`, KD `63`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `44`, knowt.com `17`
   - Proposed action: Watchlist; broad grade-calculator intent may be off-scope unless building a separate utility cluster.
9. **sat practice test** — volume `74K`, KD `73`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `48`
   - Proposed action: Watchlist or supporting content; validate SERP intent before building.
10. **ap exam schedule** — volume `60.5K`, KD `23`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `44`, knowt.com `53`, gradgpt.com `21`
   - Proposed action: Create/support an AP exam dates or score-release explainer only if aligned with scope; otherwise add FAQ module on hub.
11. **final exam calculator** — volume `49.5K`, KD `38`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `88`, knowt.com `45`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
12. **act practice test** — volume `40.5K`, KD `56`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `12`
   - Proposed action: Watchlist or supporting content; validate SERP intent before building.
13. **ap scores** — volume `33.1K`, KD `45`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `3`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.
14. **sat test dates** — volume `33.1K`, KD `49`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `4`
   - Proposed action: Watchlist or supporting content; validate SERP intent before building.
15. **act test dates** — volume `27.1K`, KD `37`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `8`
   - Proposed action: Watchlist or supporting content; validate SERP intent before building.
16. **ap test schedule** — volume `27.1K`, KD `25`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `8`, knowt.com `36`, gradgpt.com `21`
   - Proposed action: Create/support an AP exam dates or score-release explainer only if aligned with scope; otherwise add FAQ module on hub.
17. **grading calculator** — volume `27.1K`, KD `54`, intent `I`
   - Rankings evidence: ours `0`, test-ninjas.com `80`, knowt.com `45`
   - Proposed action: Create or improve subject calculator page; add exact-match title/meta, conversion table, target score module, FAQ/schema.

## Gate impact

- Semrush Keyword Gap gate can be marked **closed for domain-level missing/weak/untapped keyword discovery**.
- Remaining SEO blocker: authoritative GSC/index data for each sitemap URL is still not available without Google Search Console login/API credentials.
