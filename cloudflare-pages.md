# AP Score Calculator — Cloudflare Pages configuration

Project name: ap-score-calculator
Repository: https://github.com/godlyz/ap-score-calculator
Production branch: main
Build command: npm run build
Output directory: dist
SITE_ORIGIN: https://apscorecalculator.store
Canonical production domain: https://apscorecalculator.store

Cloudflare Pages settings checklist:

1. Build & deployments
   - Framework preset: None / static HTML
   - Build command: npm run build
   - Build output directory: dist
   - Root directory: repository root
   - Production branch: main

2. Environment variables
   - SITE_ORIGIN=https://apscorecalculator.store

3. Domain / canonical policy
   - Public canonical host: apscorecalculator.store
   - Keep both / and /ap-score-calculator-2026/ available for now. The root homepage is reachable, but its canonical tag points to /ap-score-calculator-2026/, so organic indexing should consolidate to the keyword hub.
   - Do not redirect / to /ap-score-calculator-2026/ until analytics/search-console data confirms it will not hurt users, ads, or campaign links.
   - Avoid indexing the *.pages.dev preview host when possible through Cloudflare Pages domain settings or a future host-aware redirect/robots strategy.

4. Static routing files
   - dist/_headers is generated by npm run build from scripts/build.js.
   - dist/_redirects is generated by npm run build from scripts/build.js.
